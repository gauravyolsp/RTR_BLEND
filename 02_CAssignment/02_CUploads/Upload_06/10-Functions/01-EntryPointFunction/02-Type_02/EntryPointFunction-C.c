#include <stdio.h> // 'stdio.h' contains declaration of 'printf()'

// Entery-pont function => main() => Valid return type (int) and No Parameters (void), Hence No Command-Line Arguments

int main(void)
{
	//code
	printf("Hello World !!!\n"); //Library Function
	return(0);
}
